apiVersion: argoproj.io/v1alpha1
kind: Application

metadata:
    name: demo-server
    namespace: argocd

spec:
    project: default
    source:
        repoURL: https://github.com/Archisman-Mridha/cloudnative-tools.git
        targetRevision: HEAD
        path: argo/kubernetes/applications/demo-server
    destination:
        server: https://kubernetes.default.svc
        namespace: default

    syncPolicy:
        automated:
            prune: true
            selfHeal: true