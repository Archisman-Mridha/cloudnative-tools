apiVersion: argoproj.io/v1alpha1
kind: Application

metadata:
    name: installations
    namespace: argocd

spec:
    project: default

    source:
        repoURL: https://github.com/Archisman-Mridha/cloudnative-tools
        targetRevision: HEAD
        path: argo-ecosystem/kubernetes/installations

    destination:
        server: https://kubernetes.default.svc
        namespace: default

    syncPolicy:
        automated:
            prune: true
            allowEmpty: true
            selfHeal: true