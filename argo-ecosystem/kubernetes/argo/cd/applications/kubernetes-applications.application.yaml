apiVersion: argoproj.io/v1alpha1
kind: Application

metadata:
    name: kubernetes-applications
    namespace: argocd

spec:
    project: default

    source:
        repoURL: https://github.com/Archisman-Mridha/cloudnative-tools
        targetRevision: HEAD
        path: argo-ecosystem/kubernetes/applications
        directory:
            recurse: true

    destination:
        server: https://kubernetes.default.svc
        namespace: "*"

    syncPolicy:
        automated:
            prune: true
            allowEmpty: true
            selfHeal: true